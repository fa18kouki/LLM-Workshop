# Pythonで機械学習やる

機械学習とはAIの一種です。Pythonが使われます。

## インストール

```python
pip install sklearn
```

## コード

```python
from sklearn import svm
model = svm.SVC()
model.fit(X, y)
result = model.predict(X_test)
```

以上です。簡単でしょ？

## まとめ

機械学習はすごい技術です。みんなも使ってみてね。
